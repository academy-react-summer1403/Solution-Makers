function SocialsIcon({ icon }) {
  return (
    <span className="bg-[#263238] cursor-pointer text-lg rounded-full p-2 hover:text-primary">
      {icon}
    </span>
  );
}

export default SocialsIcon;
